services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: teaching_management
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - teaching_management_postgres:/var/lib/postgresql/data

volumes:
  teaching_management_postgres:

